import {getClientEnvironment} from "@/env/client";
export const dynamic="force-static";
export function GET(){const env=getClientEnvironment();const code=`importScripts('https://www.gstatic.com/firebasejs/12.16.0/firebase-app-compat.js');\nimportScripts('https://www.gstatic.com/firebasejs/12.16.0/firebase-messaging-compat.js');\nconst CACHE='fixmate-shell-v1';\nself.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(['/offline']))));\nself.addEventListener('fetch',event=>{if(event.request.mode==='navigate')event.respondWith(fetch(event.request).catch(()=>caches.match('/offline')));});\nfirebase.initializeApp(${JSON.stringify({apiKey:env.NEXT_PUBLIC_FIREBASE_API_KEY,authDomain:env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,projectId:env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,storageBucket:env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,messagingSenderId:env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,appId:env.NEXT_PUBLIC_FIREBASE_APP_ID})});\nconst messaging=firebase.messaging();\nmessaging.onBackgroundMessage(payload=>{const notification=payload.notification||{};self.registration.showNotification(notification.title||'FixMate Pakistan',{body:notification.body||'You have a new update.',icon:'/brand/fixmate-logo.png',badge:'/brand/fixmate-logo.png',data:payload.data||{url:'/customer/notifications'}});});\nself.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.openWindow(event.notification.data?.url||'/customer/notifications'));});`;
return new Response(code,{headers:{"content-type":"application/javascript; charset=utf-8","cache-control":"public, max-age=3600","service-worker-allowed":"/"}})}
