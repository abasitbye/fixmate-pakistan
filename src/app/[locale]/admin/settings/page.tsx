import {Settings} from "lucide-react"; import {createAdminClient} from "@/lib/supabase/admin";
export default async function Page(){const {data}=await createAdminClient().from("system_settings").select("key,value,is_public,description,updated_at").order("key");return <><div className="dashboard-heading"><div><span className="section-kicker">Platform configuration</span><h1>System settings</h1><p>High-risk changes require the super administrator API.</p></div></div><div className="card-list">{(data??[]).map(setting=><article className="panel-card setting-row" key={setting.key}><span className="panel-icon"><Settings size={20}/></span><div><h2>{setting.key}</h2><p>{setting.description}</p></div><span className="status-chip">{setting.is_public?"Public":"Private"}</span></article>)}</div></>}
