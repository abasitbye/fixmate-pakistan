import {MapPin} from "lucide-react"; import {createAdminClient} from "@/lib/supabase/admin";
export default async function Page(){const {data}=await createAdminClient().from("cities").select("id,name,is_active,provinces(name),service_zones(id,name,is_active)").order("name");return <><div className="dashboard-heading"><div><span className="section-kicker">Coverage configuration</span><h1>Locations and zones</h1><p>Only explicitly configured zones are shown to applicants.</p></div></div><div className="card-list">{(data??[]).map(city=><article className="panel-card" key={city.id}><span className="panel-icon"><MapPin size={21}/></span><h2>{city.name}</h2>{city.service_zones.map(zone=><div className="review-item" key={zone.id}><span>{zone.name}</span><span className="status-chip">{zone.is_active?"Active":"Inactive"}</span></div>)}</article>)}</div></>}
