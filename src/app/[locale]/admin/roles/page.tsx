import {ShieldCheck} from "lucide-react"; import {createAdminClient} from "@/lib/supabase/admin";
export default async function Page(){const {data}=await createAdminClient().from("roles").select("id,code,name,description").order("name");return <><div className="dashboard-heading"><div><span className="section-kicker">Least privilege</span><h1>Role administration</h1><p>Only super administrators can grant or revoke elevated roles.</p></div></div><div className="card-list">{(data??[]).map(role=><article className="panel-card setting-row" key={role.id}><span className="panel-icon"><ShieldCheck size={20}/></span><div><h2>{role.name}</h2><p>{role.description}</p></div><span className="status-chip">{role.code}</span></article>)}</div></>}
