import {notFound} from "next/navigation"; import {ApplicationDecisionPanel} from "@/components/admin/application-decision-panel"; import {DocumentDecision} from "@/components/admin/document-decision"; import {createAdminClient} from "@/lib/supabase/admin";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {data}=await createAdminClient().from("professional_profiles").select("*,user_profiles!inner(display_name,email,phone,account_status),professional_documents(id,original_file_name,review_status,review_notes,verification_types(name,is_required)),professional_references(full_name,relationship,phone,verification_status),professional_services(service_subcategories(name_en))").eq("user_profile_id",id).single();if(!data)notFound();const user=data.user_profiles as unknown as {display_name:string;email:string;phone:string};const documents=data.professional_documents as {id:string;original_file_name:string;review_status:string;verification_types:{name:string}}[];const references=data.professional_references as {full_name:string;relationship:string;phone:string;verification_status:string}[];return <><div className="dashboard-heading"><div><span className="section-kicker">Application review</span><h1>{user.display_name}</h1><p>{user.email} · {user.phone}</p></div><span className="status-chip">{data.application_status.replaceAll("_"," ")}</span></div><div className="review-layout"><section className="panel-card"><h2>Professional information</h2><p>{data.bio}</p><dl className="detail-list"><div><dt>Experience</dt><dd>{data.years_experience} years</dd></div><div><dt>CNIC check</dt><dd>Ending {data.cnic_last4}</dd></div><div><dt>Tools</dt><dd>{data.has_tools?"Yes":"No"}</dd></div><div><dt>Transport</dt><dd>{data.has_transport?"Yes":"No"}</dd></div></dl></section><ApplicationDecisionPanel applicationId={id} status={data.application_status}/></div><section className="panel-card admin-section"><h2>Documents</h2>{documents.map(document=><div className="review-item" key={document.id}><div><strong>{document.verification_types.name}</strong><small>{document.original_file_name}</small></div><DocumentDecision id={document.id} status={document.review_status}/></div>)}</section><section className="panel-card admin-section"><h2>References</h2>{references.map(reference=><div className="review-item" key={reference.phone}><div><strong>{reference.full_name}</strong><small>{reference.relationship} · {reference.phone}</small></div><span className="status-chip">{reference.verification_status}</span></div>)}</section></>}
