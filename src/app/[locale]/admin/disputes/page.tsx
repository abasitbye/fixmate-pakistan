import{redirect}from"next/navigation";import{Link}from"@/i18n/navigation";import type{AppLocale}from"@/i18n/routing";import{requireAccount}from"@/lib/auth/guards";import{isMarketplaceFeatureEnabled}from"@/lib/marketplace/feature-flags";import{listDisputes}from"@/lib/marketplace/resolution/service";import{localizedPath}from"@/lib/routing/localized-path";
export default async function Page({params}:{params:Promise<{locale:string}>}){const locale=(await params).locale as AppLocale;const c=await requireAccount(locale,["admin","super_admin"]);if(!await isMarketplaceFeatureEnabled("phase2.resolution_enabled"))redirect(localizedPath(locale,"/admin"));const{data}=await listDisputes(c);return <><div className="dashboard-heading"><div><span className="section-kicker">Audited decisions</span><h1>Disputes</h1><p>Review the complete case timeline, payment hold, evidence, messages, and prior actions.</p></div></div>{data?.map(d=><Link className="panel-card setting-row" href={`/admin/disputes/${d.id}`} key={d.id}><div><h2>{d.dispute_reference}</h2><p>{d.reason_category.replaceAll("_"," ")} · {d.description}</p></div><span className="status-chip">{d.status}</span></Link>)}</>}
