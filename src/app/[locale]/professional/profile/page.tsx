import { UserRound } from "lucide-react"; import { getAuthenticatedContext } from "@/lib/auth/session";
export default async function Page(){const context=await getAuthenticatedContext();return <><div className="dashboard-heading"><div><span className="section-kicker">Professional identity</span><h1>Professional profile</h1><p>This profile remains private until application approval.</p></div></div><div className="panel-card profile-summary"><span className="profile-avatar"><UserRound size={30}/></span><div><h2>{context?.profile.display_name}</h2><p>{context?.profile.email}</p></div></div></>}
