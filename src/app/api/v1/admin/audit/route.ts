import type {NextRequest} from "next/server"; import {apiError,apiSuccess} from "@/lib/api/response"; import {getAdminContext} from "@/lib/admin/access"; import {createAdminClient} from "@/lib/supabase/admin";
export async function GET(request:NextRequest){if(!await getAdminContext())return apiError(403,"FORBIDDEN","Administrator access required.");const entity=request.nextUrl.searchParams.get("entityType");const admin=createAdminClient();let query=admin.from("audit_logs").select("id,action,entity_type,entity_id,before_data,after_data,created_at,user_profiles!audit_logs_actor_user_profile_id_fkey(display_name,email)").order("created_at",{ascending:false}).limit(200);if(entity)query=query.eq("entity_type",entity);const {data,error}=await query;if(error)return apiError(500,"AUDIT_LOAD_FAILED","Audit records could not be loaded.");return apiSuccess({audit:data});}
