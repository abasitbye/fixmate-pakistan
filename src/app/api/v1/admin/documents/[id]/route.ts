import {z} from "zod"; import {apiError,apiSuccess} from "@/lib/api/response"; import {getAdminContext} from "@/lib/admin/access"; import {documentDecisionSchema} from "@/lib/admin/schemas";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const context=await getAdminContext();if(!context)return apiError(403,"FORBIDDEN","Administrator access required.");const {id}=await params;if(!z.uuid().safeParse(id).success)return apiError(400,"INVALID_ID","Invalid document identifier.");const parsed=documentDecisionSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return apiError(400,"INVALID_DECISION","Choose a valid document decision.");const {error}=await context.supabase.rpc("review_professional_document",{target_document_id:id,target_status:parsed.data.status,decision_notes:parsed.data.notes||null});if(error)return apiError(409,"DOCUMENT_REVIEW_FAILED","The document decision could not be recorded.");return apiSuccess({status:parsed.data.status});}
