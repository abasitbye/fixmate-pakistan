import {apiSuccess} from "@/lib/api/response";import {getResolutionContext} from "@/lib/marketplace/resolution/route-utils";import {transitionDispute} from "@/lib/marketplace/resolution/service";import {resolutionCommandError} from "@/lib/marketplace/resolution/api";
export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const a=await getResolutionContext();if(!a.context)return a.response;const b=await r.json().catch(()=>({})) as {reason?:string};const{data,error}=await transitionDispute(a.context,(await params).id,"close",b.reason??"Resolution acknowledged and case closed.");return error||!data?resolutionCommandError(error):apiSuccess({dispute:data});}
