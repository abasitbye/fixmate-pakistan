import { apiError, apiSuccess } from "@/lib/api/response"; import { getAuthenticatedContext } from "@/lib/auth/session"; import { professionalServicesSchema } from "@/lib/professional/schemas";
export async function PUT(request:Request){const context=await getAuthenticatedContext();if(!context)return apiError(401,"UNAUTHENTICATED","Sign in to update services.");const parsed=professionalServicesSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return apiError(400,"INVALID_SERVICES","Select at least one valid service.",parsed.error.flatten().fieldErrors);const {error}=await context.supabase.rpc("replace_professional_services",{subcategory_ids:parsed.data.subcategoryIds});if(error)return apiError(409,"SERVICES_SAVE_FAILED","Services could not be saved for this application.");return apiSuccess({saved:true});}
