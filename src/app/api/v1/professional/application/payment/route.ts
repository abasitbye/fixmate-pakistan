import { apiError, apiSuccess } from "@/lib/api/response"; import { getAuthenticatedContext } from "@/lib/auth/session"; import { ensureEditableApplication } from "@/lib/professional/access"; import { payoutProfileSchema } from "@/lib/professional/schemas"; import { encryptSensitiveValue } from "@/lib/security/encryption"; import { createAdminClient } from "@/lib/supabase/admin";
export async function PUT(request:Request){const context=await getAuthenticatedContext();if(!context)return apiError(401,"UNAUTHENTICATED","Sign in to update payout readiness.");if(!await ensureEditableApplication(context))return apiError(409,"APPLICATION_NOT_EDITABLE","This application can no longer be edited.");const parsed=payoutProfileSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return apiError(400,"INVALID_PAYOUT_PROFILE","Check the payout information.",parsed.error.flatten().fieldErrors);const admin=createAdminClient();const {error}=await admin.from("professional_payout_profiles").upsert({professional_profile_id:context.profile.id,payout_method:parsed.data.payoutMethod,account_title:parsed.data.accountTitle,account_reference_encrypted:encryptSensitiveValue(parsed.data.accountReference),is_verified:false});if(error)return apiError(500,"PAYOUT_SAVE_FAILED","Payout readiness information could not be saved.");return apiSuccess({saved:true});}
